// Algorithm is in README.md

#include <iostream>
#include <string>

std::cons TRACK_LENGTH = 12;

srand(time(NULL));
coin = rand() % 1;

void advance(int horseNum, int* horses);
void printLane(int horseNum, int* horses);
bool isWinner(int horseNum, int* horses);

int main(){
	bool keepGoing = TRUE;
	while keepGoing(){
		if isWinner = FALSE {
			std::cout << "Press ENTER to continue! ";
		} // end if statement
	} // end keepGoing
	return 0;
} // end main
